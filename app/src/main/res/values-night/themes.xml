<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for dark mode -->
    <style name="Base.Theme.FlowBit" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- 배경 색상 -->
        <item name="android:windowBackground">@color/light_background</item>
        <item name="android:textColorPrimary">@color/black</item>

        <!-- Ripple 효과 제거 -->
        <item name="selectableItemBackground">@null</item>
        <item name="selectableItemBackgroundBorderless">@null</item>
    </style>

    <style name="Theme.FlowBit" parent="Base.Theme.FlowBit" />
</resources>